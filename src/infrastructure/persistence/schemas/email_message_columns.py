# AUTO GENERATED

class EmailMessageColumns:
    TRACKING_ID: str = "tracking_id"
    CAMPAIGN_NAME: str = "campaign_name"
    RECIPIENT_EMAIL: str = "recipient_email"
    RECIPIENT_NAME: str = "recipient_name"
    SUBJECT: str = "subject"
    SENT_AT: str = "sent_at"

    ORDER = (
        TRACKING_ID,
        CAMPAIGN_NAME,
        RECIPIENT_EMAIL,
        RECIPIENT_NAME,
        SUBJECT,
        SENT_AT,
    )
